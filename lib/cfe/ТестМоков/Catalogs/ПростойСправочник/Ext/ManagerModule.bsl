&Вместо("ПолучитьЗапрещенноеНаименование")
Функция МокПолучитьЗапрещенноеНаименование(Ид) Экспорт
	
	МассивПараметров = Новый Массив;
	МассивПараметров.Добавить(Ид);
	
	Результат = Неопределено;
	ЕстьПодмена = МокТестирование.ВыполнитьПодмену("ПолучитьЗапрещенноеНаименование", МассивПараметров, Результат);
	
	Если ЕстьПодмена Тогда
		Если ТипЗнч(Результат) = Тип("Строка") Тогда
			Возврат Результат;
		КонецЕсли;
		
		Возврат "";
	КонецЕсли;
	
	Возврат ПродолжитьВызов(Ид);
	
КонецФункции
